<nav class="w-screen bg-gray-900">
    <section class="w-screen max-w-screen-xl mx-auto py-6 h-auto flex items-center justify-between">
        <div>
            <%= nav_link_to 'Welcome', root_path, class: 'text-white p-4 rounded-md' %>
            <%= nav_link_to 'Home', home_path, class: 'text-white p-4 rounded-md' %>
            <% if user_signed_in? %>
                <%= nav_link_to 'Friends', friends_path, class: 'text-white p-4 rounded-md' %>
            <% end %>
        </div>

        <div>
            <% if user_signed_in? %>
              <%= link_to 'Sign Out', destroy_user_session_path, data: {  }, class: 'text-white p-4 rounded-md' %>
            <% else %>
              <%= link_to 'Sign up', new_user_registration_path, class: 'text-white p-4 rounded-md' %>
              <%= link_to 'Sign In', new_user_session_path, class: 'text-white p-4 rounded-md' %>
            <% end %>
        </div>
    </section>
</nav>